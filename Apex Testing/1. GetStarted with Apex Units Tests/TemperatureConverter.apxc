public class TemperatureConverter {
	// Takes a Farenheit temperature and returns the Celsius equivalent
    public static Decimal FarenheitToCelsius(Decimal fh) {
        Decimal cs = (fh -32)*5/9;
        return cs.setScale(2);
    }
}
